import { Text } from "react-native";

const CareerStatValue = ({ value }) => {
    return <Text className="w-12 text-center py-3.5 text-xs font-medium inline-block">{value}</Text>;
};

export default CareerStatValue;
